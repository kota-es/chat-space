.chat-side
  .side-header
    .side-header__box
      %p.side-header__box__username 
        =current_user.name
      %ul.side-header__box__menu
        %li.side_header__box__new-group
          =link_to new_group_path do
            = fa_icon 'pencil-square-o'
        %li.side_header__box__edit-account
          =link_to edit_user_path(current_user) do
            = fa_icon 'cog'
  .side-groups
    - current_user.groups.each do |group|
      .group-box
        = link_to '#' do
          %p.group-box__name 
          = group.name
          %p.group-box__latest-message 
            メッセージはまだありません。